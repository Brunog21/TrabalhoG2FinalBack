# =====================================================================
# application-test.properties — auth-service
# Perfil ativo: test
# Spring Boot 4.0.6 | Java 25 | Spring Security + JWT
# =====================================================================

# --- Eureka desabilitado ---
eureka.client.enabled=false
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# --- H2 em memória (SpringBootTest / WebMvcTest) ---
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# --- DDL gerenciado pelo Hibernate (sem Flyway) ---
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# --- Flyway desabilitado (SQL usa SERIAL, incompatível com H2) ---
spring.flyway.enabled=false

# --- Porta do serviço ---
server.port=8900